# 测试说明

## 已完成

- 前端模块目录断言脚本：校验三类增值服务占位模块存在且默认未启用
- 前端生产构建通过：`npm.cmd run build --workspace apps/web`
- 后端测试通过：`mvn -q "-Dmaven.repo.local=.m2repo" -pl apps/api test`
- 后端测试覆盖：
  - 预约创建接口
  - 后台登录接口
  - 平台总览接口
  - 前后端分离场景的 CORS 放行

## 待补充

- 联调预约表单与后台列表
- 后台未登录拦截的浏览器级验证
- 使用真实 MySQL 容器启动后跑一次端到端流程
- 接入真实预约 API 后补适配器联调测试

## 当前风险

- 当前尚未实际启动 MySQL 容器做人工联调
- 后续若接真实预约 API，需要增加适配层联调测试
