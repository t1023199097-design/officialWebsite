# AI 协作记录文档（M8b）

> 提交前 ≥ 500 字。这份文档记录"AI 是如何参与到项目的"。

## 我们用了哪些 AI 工具？

- Claude Code（主要）
- Cursor / Copilot / v0 / 其它

## 关键的 AI-人协作时刻

### 时刻 1：…

任务：
AI 给的初稿：
我们改了什么：
原因：

### 时刻 2：…

…

### 时刻 3：…

…

## 一个反思

如果重做，会怎么改进 AI 协作流程？

## 时刻 4：官網 Spec V2.0 參賽提交版整理

任务：
根据团队最新讨论，将 Beefintech -- Lifebee 官网需求文档调整为正式参赛提交版。官网定位从产品功能说明收敛为品牌推广、潜在客户吸引、市场成果展示和预约咨询转化。

AI 给的初稿：
AI 以产品经理和技术文档工程师角色重新整理文档结构，输出标准 Spec 格式，包含文件信息、版本记录、项目背景、项目目标、官网定位、目标用户、核心价值主张、需求范围、视觉设计要求、页面清单、页面需求、基础功能、适配要求、交付范围和验收标准。

我们改了什么：
1. 首页信息架构调整为品牌功能介绍、市场业务规模、预约咨询演示、关于我们四个维度。
2. 品牌功能介绍只保留大模块表达，不继续罗列平台小功能和操作说明。
3. 增值服务范围只保留内容服务类，即保险科技资讯和行业政策解读专栏。
4. 删除客户赋能类和工具服务类方向，包括 FAQ、自助查询、在线工单、更新日志、ROI 计算器、行业术语词典等内容。
5. 同步生成项目内归档文档 `docs/official-website-requirements-v2.0.md`。

原因：
官网第一阶段的核心任务是让潜在客户快速理解 LifeBee 的品牌价值、服务成果和预约路径，不适合写成平台操作手册或功能清单。收敛后的 Spec 更适合作为参赛材料、设计执行依据和后续开发验收依据。

## 时刻 5：Execution document 执行计划整理

任务：
根据桌面版执行文档，将原本从视觉设计、页面需求、基础功能、适配要求、验收标准延伸出来的内容整理为可提交的 Execution document，并补齐 M4 要求的 marker、至少 4 个 Phase 和任务清单。

AI 给的初稿：
AI 先读取 `执行文档.md` 与仓库内 `docs/plan.md`，确认比赛模板要求是 `generated-by: superpowers-writing-plans` marker、Phase 结构和 checkbox 任务清单。随后在不破坏原文档格式的前提下，将文档序号从 1 开始重排，并在末尾补充实施计划。

我们改了什么：
1. 在执行文档顶部加入 `<!-- generated-by: superpowers-writing-plans -->` marker。
2. 将章节序号从 1 开始重新排列，保持原有「章节 + 小节 + 表格 + 编号清单」格式。
3. 增加 `实施计划` 章节，拆分为首页骨架与品牌视觉、核心页面内容建设、预约咨询演示与 API、适配验收与提交 4 个 Phase。
4. 每个 Phase 均使用 checkbox 任务清单，便于后续开发和验收逐项推进。
5. 将最终文件以英文名称保存为 `docs/Execution document.md`，便于仓库提交和团队识别。

原因：
Execution document 的用途是把需求文档转化为可以执行和验收的阶段任务。保留原有格式能避免文档结构被打散；补齐 marker、Phase 和任务清单则能满足比赛 M4 文档要求，并让后续开发团队更容易按阶段推进。

## 时刻 6：Spec.md 纯需求文档生成

任务：
根据团队要求，从 `docs/design.md` 中生成一份更干净的 Spec 需求文档，并去掉 `目标` 章节之前的模板 marker、标题说明和文档信息表。

AI 给的初稿：
AI 先定位 `docs/design.md` 中 `## 1. 目标` 所在行，确认该标题之前包含 `superpowers-brainstorming` marker、Design Spec 标题、模板说明和文档信息表。随后从 `## 1. 目标` 开始截取到文件结尾，生成新的 `docs/Spec.md`。

我们改了什么：
1. 保留原 `docs/design.md` 不变，避免影响 M3 设计文档模板提交。
2. 新增 `docs/Spec.md`，内容从 `## 1. 目标` 开始。
3. 删除新文件中 `目标` 前面的 marker、标题、说明和文档信息表。
4. 保留目标、页面架构、核心功能、Tech Stack、移动端适配等正式需求内容。

原因：
`design.md` 适合保留比赛模板和设计文档上下文，但单独提交或传阅 Spec 时，前置模板信息会显得冗余。生成 `Spec.md` 可以让需求内容更直接、更适合团队查看和后续交付引用。

## 时刻 7：同步官网页面到 xiaoxiao 分支

任务：
将 `xiaoyu` 分支当前可预览的 LifeBee 官网页面同步到 `xiaoxiao` 分支，并明确保留首页右侧水滴球体视觉。

AI 给的初稿：
AI 先检查 `xiaoyu` 与 `xiaoxiao` 分支结构差异，确认 `xiaoyu` 包含完整 `apps/web` 官网代码，而 `xiaoxiao` 原本主要是比赛文档骨架。随后选择只同步前端官网运行所需文件，不覆盖 `xiaoxiao` 原有 README、milestone、团队配置和比赛文档。

我们改了什么：
1. 将 `apps/web` 前端官网代码同步到 `xiaoxiao` 分支。
2. 同步根目录 `package.json` 与 `package-lock.json`，保证本地可通过相同方式启动官网。
3. 保留首页 `hero-visual`、`waterdrop-field`、`waterdrop-main`、`waterdrop-secondary` 和 `waterdrop-glint` 相关结构与样式。
4. 启动 `http://127.0.0.1:5173/` 进行本地验证，确认首页、简体版和英文版均可访问。

原因：
团队后续需要在 `xiaoxiao` 分支继续查看和维护当前官网视觉版本。如果只保留文档而没有前端页面代码，新的对话或协作成员切到 `xiaoxiao` 后无法打开同一版官网，因此需要把当前已确认的页面同步到该分支。

## 时刻 8：根据 design.md 调整官网展示内容与功能

任务：
在 `xiaoxiao` 分支继续完善 LifeBee 官网，要求严格根据 `docs/design.md` 的需求文档调整页面展示部分和功能，同时保持当前网站设计不变，尤其不能改动首页右侧水滴球体。

AI 给的初稿：
AI 先读取 `docs/design.md`、首页 `apps/web/app/page.js`、站点内容数据、导航、页脚、预约表单和现有样式文件，判断当前首页已经形成独立视觉版本。AI 没有直接重做页面，而是提出“只改内容与功能层，不改首页视觉结构”的方案：保留首页布局、动效和水滴球体；补齐需求文档中的页面架构；强化预约表单；将前台文案从比赛骨架语气调整为正式品牌官网表达。

我们改了什么：
1. 保持首页 `apps/web/app/page.js` 不变，未改动 `hero-visual`、`waterdrop-field`、`waterdrop-main`、`waterdrop-secondary`、`waterdrop-glint` 等首页水滴球体结构。
2. 新增 `/market-scale` 市场业务规模页，用于展示服务伙伴、市场覆盖、服务成果和数据口径说明。
3. 新增 `/insights` 内容服务页和 `/insights/:slug` 内容详情页，聚焦保险科技资讯与行业政策解读。
4. 新增 `/contact` 联系我们页，承接商务合作、渠道合作、客户服务和一般联系需求。
5. 新增 `not-found.js`，让 404 页面提供返回首页、预约演示和联系我们入口。
6. 将 `/services` 调整为品牌功能介绍页，围绕展业效率提升、经营管理提效、流程协同与数据赋能、长期服务支持四个大模块表达。
7. 保留 `/solutions` 作为兼容入口，但复用品牌功能介绍页内容，避免旧入口失效。
8. 将导航和页脚从 Admin / 比赛骨架语气改为正式 LifeBee 官网入口和品牌说明。
9. 按需求文档强化 `/demo` 预约表单，补充姓名、公司名称、联系电话、预约时间、备注，以及公司所在地、团队规模、希望了解的服务方向等可选字段。
10. 为预约表单增加前端必填校验、电话格式校验、预约时间校验、提交中状态、防重复提交、成功提示和失败提示。
11. 保持 API 调用可替换，继续使用 `NEXT_PUBLIC_API_BASE_URL`，同时提交文档建议字段结构中的 `name`、`company`、`phone`、`appointmentTime`、`message`、`region`、`teamSize`、`interest`。
12. 扩展 `apps/web/src/lib/module-catalog.test.js`，让新增站点内容、文章数据、市场规模内容和联系渠道有基础测试覆盖。

原因：
`design.md` 的目标是把 LifeBee 官网从基础展示页升级为品牌推广与商务转化入口，而不是重做视觉设计。当前首页右侧水滴球体已经是团队确认过的视觉重点，因此 AI 将工作边界收敛到“页面展示内容、信息架构和表单功能”，避免破坏现有首屏体验。新增页面让官网符合需求文档中的首页、品牌功能介绍、市场业务规模、预约咨询演示、关于我们、内容服务、联系我们和 404 架构；强化表单则让官网具备实际转化能力。

验证：
AI 在修改后执行了 `npm run test --workspace apps/web`，确认内容数据测试通过；执行 `npm run build --workspace apps/web`，确认 Next.js 生产构建成功；重启 `http://127.0.0.1:5173/` 预览服务后，检查 `/`、`/services`、`/market-scale`、`/insights`、`/insights/insurance-brokerage-digital-operations`、`/contact`、`/demo` 均返回 200，未知页面返回 404。

## 时刻 9：首页下滑导航、翻转卡片与功能服务视觉迭代

任务：
在不改动首页首屏设计、不影响右侧水滴球体的前提下，把顶部导航与首页下滑内容建立对应关系，并在首页下滑第一部分加入更有产品感的功能服务视觉展示。

AI 给的初稿：
AI 先根据用户截图理解导航栏需要作为首页内部定位入口，而不是重做首屏。最初 AI 做了导航定位效果图，随后根据反馈收敛范围：首屏当前显示内容完全不动，只处理首屏之后的展示内容。AI 先把导航改成首页锚点，再把下滑内容从普通区块调整为翻转卡片，并逐步根据用户反馈优化卡片尺寸、排列方式、间距和产品视觉。

我们改了什么：
1. 将顶部导航改为首页内锚点，对应 `品牌功能`、`市场规模`、`内容服务`、`关于我们`、`联系我们` 五个下滑模块。
2. 删除首页首屏下方原本的能力背书横栏，即 `经纪营运 / 顾问 APP / 管理后台 / 保司 API / 数据金库`。
3. 保留翻转卡片交互效果，但把五张卡片从横向聚集改为纵向逐一排列展示。
4. 每个翻转卡片下方都紧跟对应模块的信息说明，包括品牌功能、市场规模、内容服务、关于我们、联系我们的具体展示内容。
5. 根据反馈删除翻转卡片上的数字标注，让卡片更干净。
6. 收紧首屏与下滑第一部分之间的空隙，让页面衔接更紧凑。
7. 在首页下滑第一部分新增 `功能服务` 视觉段，使用 CSS 绘制 PC 管理台 + 手机功能菜单的产品示意。
8. 为功能服务视觉增加由暗到亮的屏幕内扫光动画，同时移除背景移动灰色框，避免视觉干扰。
9. 将产品视觉外框从普通设备示意调整为台式电脑显示器，并加入显示器边框、底座、内屏凹槽、手机边框、听筒和玻璃高光等质感细节。
10. 扩展 `site-content.mjs` 中的首页锚点、翻转模块和功能服务视觉数据，并在 `module-catalog.test.js` 中增加基础断言。

原因：
用户希望首页首屏已经确认的视觉保持稳定，只让下滑内容更清楚地承接导航栏。翻转卡片适合作为模块入口，但如果全部挤在一起会降低阅读节奏，因此最终改为纵向逐段叙事。功能服务视觉用于在进入卡片之前先给访客一个产品能力的直观印象；设备外框和屏幕扫光则增强科技产品质感，同时不破坏原有科技金融蓝视觉体系。

验证：
AI 每轮关键调整后都执行 `npm run test --workspace apps/web`，确认内容数据断言通过；执行 `npm run build --workspace apps/web`，确认 Next.js 生产构建成功；重启 `http://127.0.0.1:5173/` 预览服务后，检查首页与 `/#brand-functions` 锚点返回 200。

## 时刻 10：恢复整个首页页面并重新拉起本地预览

任务：
用户要求把对话里这版首页“整个页面”恢复到截图对应状态，而不是只恢复 Hero 按钮。随后又反馈网页打不开，希望先把本地预览重新拉起来并确认可访问。

AI 给的初稿：
AI 先核对当前仓库状态，确认首页页面代码、样式、站点数据和测试文件都还保留着截图那一版的结构。随后 AI 选择只回退后来被误恢复成两按钮状态的局部改动，把 Hero 区重新调回三个按钮版本，并保留首页首屏、右侧水滴球体、下滑功能服务视觉和后续展示模块。页面打不开时，AI 进一步重启 5173 开发服务，直接用浏览器重新打开首页做可访问性验证。

我们改了什么：
1. 将首页 Hero 区恢复为三个按钮：`品牌功能`、`市場規模`、`關於我們`。
2. 恢复对应的 `hero-action-button` 样式，让这三个按钮保持略大的显示比例。
3. 保留首页右侧水滴球体、下滑 `功能服務` 视觉、电脑 + 手机展示、以及下方五个展示模块，不再回退这些页面结构。
4. 重启 `http://127.0.0.1:5173/` 开发服务，清理旧的 `.next` 缓存，解决页面打不开的问题。
5. 在浏览器中重新打开首页，确认页面标题、URL、Hero 按钮数量与按钮文案都正常。
6. 把这次协作过程补写进 `docs/ai-collab-log.md`，让项目协作记录与实际操作同步。

原因：
用户这次要恢复的不是单个按钮，而是当时整版首页的状态；而“网页已打不开”又说明需要同时处理本地预览恢复。把这次过程写进协作记录，可以让后续进入 `xiaoxiao` 分支的人知道：这版首页是经过多轮确认、恢复、验证后定下来的，不是随手拼出来的临时态。

验证：
AI 重新运行 `npm run test --workspace apps/web` 与 `npm run build --workspace apps/web`，确认测试和生产构建都通过；随后重启本地开发服务并用浏览器再次打开 `http://127.0.0.1:5173/`，确认首页可访问，标题正常，Hero 区按钮数量和文案都符合截图对应版本。

## 时刻 11：产品服务页衔接旧版水滴首页与电脑展示

任务：
用户希望 `/product-services` 页面不要从 PDF 风格的功能服务页直接开始，而是先恢复之前确认过的首页首屏和第二屏：首屏包含右侧水滴特效、模块流动展示和品牌主张；第二屏包含 PC 管理台与手机功能面板。PDF 参考只用于后续产品服务功能介绍部分，不应覆盖旧版首页视觉。

AI 给的初稿：
AI 先从 Git 历史中查找旧版首页代码，定位到包含 `waterdrop-field`、`hero-module-stage`、`feature-laptop`、`feature-phone` 的页面结构和样式。随后 AI 判断用户一直查看的是 `/product-services`，而不是 `/` 首页，因此仅恢复首页本身并不能解决用户看到的问题。正确方案是将旧版首页的前两屏作为 `/product-services` 的开头，再把现有产品服务内容接在后面。

我们改了什么：
1. 在 `apps/web/app/product-services/page.js` 引入 `getDemoHomeContent`、`homeFeaturePreview` 和 `normalizeLocale`，复用旧首页的数据源。
2. 将旧版水滴首屏结构接入 `/product-services`，包括品牌文案、预约演示入口、查看功能服务锚点、右侧水滴元素和横向模块流动展示。
3. 将旧版第二屏的功能服务视觉接入 `/product-services`，包括 PC 管理台、数据卡片、柱状图、环形图、手机功能菜单和扫光装饰。
4. 保留后续 `產品功能服務升級`、香港市场、会员功能、营运基础设施、每月产值和 AI 代理服务等 PDF 衔接内容，让页面形成“旧版首页视觉 → 产品展示第二屏 → 功能服务长页面”的连续结构。
5. 在 `apps/web/app/globals.css` 增加少量衔接样式，控制前两屏与后续产品服务区块的间距，并收紧后续功能服务页面字号和卡片密度。
6. 排查用户反馈“网页打不开”的原因，确认不是页面代码缺失，而是 Next.js dev 服务的 `.next` 缓存损坏，报错为 `Cannot find module './662.js'`。
7. 停止旧的 `next dev` 进程，清理 `.next` 缓存，重新以 `npm run dev -- --port 5173` 启动本地服务。

原因：
用户要恢复的是一个已经确认过的视觉叙事顺序，而不是单独修某个区块。若只调整 `/` 首页，用户打开 `/product-services` 仍会看不到水滴和电脑展示；若把 PDF 风格内容放在最前，又会破坏原来首页的视觉记忆。最终采用“前两屏恢复旧版，后续接 PDF 功能服务”的方案，既保留原始水滴品牌视觉，也满足后续产品服务介绍的需求。

验证：
AI 执行 `npm test`，确认内容断言通过；执行 `npm run build`，确认生产构建成功。随后通过 `curl -I http://127.0.0.1:5173/product-services` 验证页面返回 `200`，并用浏览器 DOM 快照确认页面开头已经包含水滴首屏、模块流动展示、PC 管理台和手机功能面板。最后重新清理 `.next` 并启动 5173 本地开发服务，确认 `/product-services` 不再返回 500。

## 时刻 12：保留当前设计并恢复功能服务后续页面

任务：
用户在多轮尝试功能服务分支树、小工具组件和动效后，要求保存当前确认版本：恢复分支树之前的完整产品服务页面，保留旧版水滴首页与电脑展示衔接，同时将第二屏标题从 `功能服務` 改为 `快人一步 數碼保險化`。

AI 给的初稿：
AI 先尝试参考 Stripe 的视觉语言，把功能服务详情做成分支树与小工具组件动效；用户反馈后续页面丢失、仍未恢复。AI 随后停止继续叠加视觉实验，改为回到当前稳定版本：撤掉分支树组件、删除分支树相关 CSS、恢复 `ps-capability-grid` 四张功能卡片，并保留产品服务页面后续市场、会员、基础设施、每月产值与 AI 服务区块。

我们改了什么：
1. 移除 `/product-services` 中的 `ServiceBranchTree` 实验组件引用，恢复原来的四张功能服务卡片。
2. 删除分支树和小工具功能组件相关文件与样式，避免影响后续页面显示和滚动节奏。
3. 保留 `/product-services` 前两屏旧版设计：水滴首屏、横向模块流动展示、PC 管理台与手机功能面板。
4. 将第二屏展示标题从 `功能服務` 改为 `快人一步 數碼保險化`。
5. 同步更新 `module-catalog.test.js` 中对 `homeFeaturePreview.title` 的断言。
6. 清理 `.next` 开发缓存并重启 `http://127.0.0.1:5173`，避免浏览器或 Next.js 继续显示旧实验版本。

原因：
用户最终确认要保存的是稳定页面，而不是继续实验分支树效果。相比复杂动效，当前阶段更重要的是保证首页视觉、第二屏产品展示和后续产品服务内容完整连贯。保留 `快人一步 數碼保險化` 可以满足最新文案要求，同时恢复四张功能卡能确保页面不再因为实验组件导致内容缺失或视觉混乱。

验证：
AI 执行 `npm test` 和 `npm run build`，确认测试与生产构建通过；随后清理 `.next` 并重启 5173 预览服务。通过 `curl` 和浏览器 DOM 快照确认 `/product-services?restore=1` 返回 200，页面中没有 `ServiceBranchTree` 或 `功能服務分支樹`，保留 `快人一步 數碼保險化`，并且 `產品功能服務升級`、香港市场、会员功能、每月产值、AI 服务等后续区块都存在。

## 时刻 13：产品功能服务升级四卡视觉重绘

任务：
用户提供一张四栏功能卡片设计图，希望将 `/product-services` 页面中 `產品功能服務升級` 下方 4 张卡片尽量 1:1 还原，并加入适度动效。随后用户反馈上方大数字容易干扰阅读，要求取消数字并让文字整合分布显示，避免重合。

AI 给的初稿：
AI 先把原来的普通小设备卡片改成参考图式的纵向功能海报：柔和渐变底、居中大标题、三行说明和底部业务插画，并用 CSS 绘制保单同步、电子投保、资料库和邀约申请四组插画，同时加入 hover 上浮、光带扫过和插画轻微浮动。根据用户反馈，AI 再移除背景大数字，把 `AI / Eform` 与主标题合并成稳定标题组，并重新划分标题区、说明区和插画区高度。

我们改了什么：
1. 将四张卡片内容改为 `AI 保單同步`、`Eform 無紙化投保`、`合規資料庫`、`邀約神器`。
2. 更新每张卡片的三行说明文案，使其贴近用户提供的参考图。
3. 在 `product-services/page.js` 中新增 `CapabilityIllustration`，按卡片类型渲染不同的 CSS 插画结构。
4. 在 `globals.css` 中重写 `ps-cap-card` 视觉：柔和渐变、圆角边框、轻阴影、hover 上浮、光带扫过和插画浮动动画。
5. 取消卡片上方大数字显示，避免标题与数字重叠。
6. 将 `AI`、`Eform` 标签整合到标题内部，并为标题、说明、底部插画分配固定空间。
7. 增加 1180px 与 900px 以下的响应式规则，让四栏、两栏、单栏状态下文字和插画都不重合。

原因：
参考图的核心不是单纯卡片列表，而是功能海报式的“标题 + 说明 + 场景插画”组合。第一版大数字能接近参考图，但实际页面中会削弱文字清晰度；用户明确要求取消后，最终设计改为更干净的标题组，同时保留柔和渐变、插画和动效来延续参考图的视觉气质。

验证：
AI 执行 `npm test`，确认 `module catalog assertions passed`；执行 `node apps/web/src/lib/product-services-content.test.js`，确认 `product services content assertions passed`。随后通过 `curl -I 'http://127.0.0.1:5173/product-services?restore=1778844893172'` 验证页面返回 200，并在浏览器中刷新当前页面确认新版卡片内容已出现。

## 时刻 14：产品服务长页市场、内容、ROI 与三语切换完善

任务：
用户希望继续优化 `/product-services` 后续页面：将市场区块改为 `市場規模`，突出香港、新加坡、亚太海外市场份额与增长；将原本三段内容整合为 `內容服務` 模块，并进一步把其中一页做成可在线交互使用的 ROI 计算器，把 `港險資訊一覽` 放在下一页；再参考 hkinsu.com 放入几条香港保险市场资讯；最后把尾段改为 `預約演示 / 聯絡我們` 并全局校验简、繁、英语言切换。

AI 给的初稿：
AI 先把旧的香港市场板块改成市场规模仪表板，用香港、新加坡、亚太海外三组占比和增长条表达市场版图。随后将内容服务区域重组为 ROI 计算器、港险资讯一览和预约联系三个层次，并把 ROI 计算逻辑独立成可测试函数与客户端组件。最后排查语言切换，发现 Header 原本会从任何页面跳回首页，且产品服务页后半段只有繁体数据，于是补齐路径保留逻辑和三语内容数据。

我们改了什么：
1. 将市场区块标题改为 `市場規模`，加入香港、新加坡、亚太海外三组市场占比、增长示意和进度条动画。
2. 新增 `RoiCalculator` 客户端组件和 `calculateRoiProjection` 计算函数，让用户可拖动每月保费、持有年期、预期年化回报和保障倍数，并即时查看总投入、预估价值、预估收益、ROI 与保障额度。
3. 将 `港險資訊一覽` 移到 ROI 计算器下一段，并在右侧资讯面板中加入参考 hkinsu.com 信息主题的条目：每日财经新闻、跨行业背景查核、分红险收入来源和四大保司业绩对比。
4. 将尾段从 AI 回报示意改为 `預約演示 / 聯絡我們`，展示电话、WhatsApp、邮箱和香港九龙塘创新中心地址，并提供可点击的电话、WhatsApp、邮件和预约演示入口。
5. 把 Header 改为客户端感知当前路径和 query 的语言切换：繁体、简体、英文切换时保留当前页面路径，不再跳回首页。
6. 为产品服务长页补齐繁体、简体和英文内容映射，并让 ROI 计算器的输入项与结果标签按语言切换。
7. 为 Header 使用 `useSearchParams` 后补上全局 `Suspense` 包裹，解决 Next.js 生产构建对 CSR bailout 的要求。
8. 更新 `module-catalog.test.js` 和 `product-services-content.test.js`，覆盖市场规模、ROI 计算结果、预约联系信息和三语内容入口。

原因：
用户已经确认产品服务页的前两屏和四张功能卡片，后续重点转向信息架构与转化路径：市场规模负责建立机会感，ROI 计算器负责互动评估，港险资讯负责内容沉淀，预约与联系方式负责承接咨询。语言切换必须在当前页面内准确对应，否则英文或简体访客会被带回首页或看到混合语言内容，影响专业度和可用性。

验证：
AI 先为 ROI 计算函数写测试并确认缺少模块时测试失败，然后实现计算逻辑与组件。最终执行 `npm test`、`node apps/web/src/lib/product-services-content.test.js` 和 `npm run build` 均通过。随后清理 `.next` 缓存、重启 `http://127.0.0.1:5173`，逐个请求 `/`、`/?lang=zh-Hans`、`/?lang=en`、`/product-services`、`/product-services?lang=zh-Hans`、`/product-services?lang=en`，确认全部返回 200，关键文案与语言切换链接均准确对应。
