spring:
  datasource:
    url: jdbc:h2:mem:beefintech;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  jackson:
    time-zone: Asia/Shanghai

app:
  admin:
    username: admin
    password: ChangeMe123!
    display-name: Platform Admin
  web:
    allowed-origins: http://localhost:3000,http://127.0.0.1:3000
  storage:
    object-dir: ./var/test-storage
